import { NgImageUploadDirectiveDirective } from './ng-image-upload-directive.directive';

describe('NgImageUploadDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgImageUploadDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
